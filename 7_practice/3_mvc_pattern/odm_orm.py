class AMR_model:
	OD_table _odt

	def search_path(self, name):
		cost_table = calc_cost_from_od_links(self._odt)

		for...

	[table='od_link', where='']
	def serialize(self, load, fields): # ORM
		_odt = parse(fields)



